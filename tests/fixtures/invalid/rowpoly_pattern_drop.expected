add_score: FAIL
  error: [pple-rowpoly-not-preserved] @rowpoly: helper does not preserve row variable 'R' (parameter 'df') — the frame returned at line 51 is produced by a column-reducing select/drop keyed by a pattern or selector (regex / dtype / name-pattern), which could drop a caller extra that happens to match. Keep every column (an all-columns form like pl.all() / cs.all(), or reduce by explicit known names only), or remove 'R' from @rowpoly.
