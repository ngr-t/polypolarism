enrich: WARN
  warning: [PLW008] annotation `DataFrame[Enriched]` narrows the inferred schema without runtime backing: column 'segment': declared Int64 but inferred Int64?. Assert it with `Schema.validate(...)` (which retypes the value), or widen the annotation.
