smooth: WARN
  warning: [PLW007] `.interpolate()` is not modeled by polypolarism — the frame's schema is no longer tracked and downstream checks go silent. Validate the result against a schema (`Schema.validate(...)`) to keep checking precise.
